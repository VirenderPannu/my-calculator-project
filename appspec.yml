version: 0.0
os: linux
files:
  - source: dist/my-calculator-project
    destination: /var/www/my-calculator-project
permissions:
  - object: /var/www/my-calculator-project
    pattern: '**'
    mode: '0755'
    owner: root
    group: root
    type:
      - file
      - directory
hooks:
  ApplicationStart:
    - location: deploy-scripts/application-start-hook.sh
      timeout: 300
      